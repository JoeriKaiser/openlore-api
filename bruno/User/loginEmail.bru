meta {
  name: loginEmail
  type: http
  seq: 2
}

post {
  url: {{apiUrl}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "john.donut@example.com",
    "password": "password123"
  }
}

settings {
  encodeUrl: true
}
